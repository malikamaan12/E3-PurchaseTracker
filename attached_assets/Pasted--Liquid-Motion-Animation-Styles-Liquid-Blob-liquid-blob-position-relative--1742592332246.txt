/* Liquid Motion Animation Styles */

/* Liquid Blob */
.liquid-blob {
  position: relative;
  overflow: hidden;
  border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
  animation: morphBlob 8s ease-in-out infinite alternate;
}

@keyframes morphBlob {
  0% {
    border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
  }
  25% {
    border-radius: 58% 42% 75% 25% / 76% 46% 54% 24%;
  }
  50% {
    border-radius: 50% 50% 33% 67% / 55% 27% 73% 45%;
  }
  75% {
    border-radius: 33% 67% 58% 42% / 63% 68% 32% 37%;
  }
  100% {
    border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
  }
}

/* Liquid Fill */
.liquid-fill {
  position: relative;
  overflow: hidden;
}

.liquid-fill::before {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 0;
  background: hsla(var(--primary), 0.2);
  animation: liquidFill 2.5s ease-in-out forwards;
  z-index: -1;
}

@keyframes liquidFill {
  0% {
    height: 0;
    transform: translateY(0) scaleY(1);
  }
  40% {
    height: 100%;
    transform: translateY(0) scaleY(1);
  }
  60% {
    height: 100%;
    transform: translateY(-15%) scaleY(1.1);
  }
  80% {
    height: 100%;
    transform: translateY(-7%) scaleY(1.05);
  }
  100% {
    height: 100%;
    transform: translateY(0) scaleY(1);
  }
}

/* Liquid Wave Text */
.liquid-wave-text {
  position: relative;
  display: inline-block;
}

.liquid-wave-text > span {
  display: inline-block;
  animation: waveText 2s ease-in-out infinite;
  animation-delay: calc(0.1s * var(--i, 0));
}

@keyframes waveText {
  0%, 100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-10px);
  }
}

/* Liquid Ripple */
.liquid-ripple {
  position: relative;
  overflow: hidden;
}

.liquid-ripple::after {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 0;
  height: 0;
  background: radial-gradient(circle, hsla(var(--primary), 0.4) 0%, transparent 70%);
  border-radius: 50%;
  transform: translate(-50%, -50%);
  opacity: 0;
  animation: liquidRipple 1.5s cubic-bezier(0.25, 0.8, 0.25, 1) infinite;
}

@keyframes liquidRipple {
  0% {
    width: 0;
    height: 0;
    opacity: 0.8;
  }
  100% {
    width: 200%;
    height: 200%;
    opacity: 0;
  }
}

/* Liquid Gradient Background */
.liquid-gradient {
  background: linear-gradient(-45deg, hsla(var(--primary), 0.7), hsla(var(--secondary), 0.7), hsla(var(--accent), 0.7));
  background-size: 400% 400%;
  animation: liquidGradient 15s ease infinite;
}

@keyframes liquidGradient {
  0% {
    background-position: 0% 50%;
  }
  50% {
    background-position: 100% 50%;
  }
  100% {
    background-position: 0% 50%;
  }
}

/* Liquid Button Effect */
.liquid-btn {
  position: relative;
  overflow: hidden;
  transition: all 0.3s ease;
}

.liquid-btn::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(
    90deg,
    transparent,
    hsla(var(--primary), 0.2),
    transparent
  );
  animation: liquidBtnShine 3s linear infinite;
}

@keyframes liquidBtnShine {
  0% {
    left: -100%;
  }
  100% {
    left: 100%;
  }
}

/* Liquid Floating Animation */
.liquid-float {
  animation: liquidFloat 3s ease-in-out infinite alternate;
}

@keyframes liquidFloat {
  0% {
    transform: translateY(0);
  }
  100% {
    transform: translateY(-10px);
  }
}

/* Liquid Drip */
.liquid-drip {
  position: relative;
}

.liquid-drip::after {
  content: '';
  position: absolute;
  bottom: -15px;
  left: 50%;
  width: 15px;
  height: 15px;
  background: hsla(var(--primary), 0.7);
  border-radius: 50% 50% 50% 50% / 60% 60% 40% 40%;
  transform: translateX(-50%);
  animation: liquidDrip 2s ease-in infinite;
}

@keyframes liquidDrip {
  0% {
    bottom: 0;
    height: 5px;
    width: 5px;
    opacity: 1;
    border-radius: 50% 50% 50% 50% / 60% 60% 40% 40%;
  }
  70% {
    bottom: -20px;
    height: 15px;
    width: 10px;
    opacity: 0.7;
    border-radius: 50% 50% 40% 40% / 40% 40% 60% 60%;
  }
  100% {
    bottom: -25px;
    height: 5px;
    width: 5px;
    opacity: 0;
    border-radius: 50%;
  }
}